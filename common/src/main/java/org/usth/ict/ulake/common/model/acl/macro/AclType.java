package org.usth.ict.ulake.common.model.acl.macro;

/**
 * Combinator of character to detect user and file type with
 * g (group user), u (single user), d (folder), f (file).
 * */
public enum AclType { gd, gf, ud, uf }
